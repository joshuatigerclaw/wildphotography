{
  "buildCommand": "cd apps/web && npm install && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "apps/web",
      "use": "@vercel/next"
    }
  ]
}
